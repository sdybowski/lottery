import React from 'react';
import _ from 'lodash';

export const Gambler = ({ mode, draw }) => {
	return <section>
		<button onClick={draw}>Draw</button>
		<p>
			<span>ddd</span>
		</p>
	</section>
};
